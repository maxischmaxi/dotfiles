# pi-personal

Persönliche Pi-Commands.

## Commands

- `/clear` — Alias für `/new`: startet eine neue Session. Der alte Verlauf
  bleibt als Session-File erhalten (wie bei `/new` auch, aufrufbar mit
  `/resume`).

## Installation

```bash
pi install /home/max/Projects/pi-personal
```